# Blockchain node (FastAPI + SQLAlchemy async + PostgreSQL)
# Install: pip install -r requirements.txt

pydantic~=2.12.5
pydantic-settings==2.12.0
python-dotenv>=1.0
fastapi~=0.128.0
starlette~=0.50.0
uvicorn~=0.40.0
requests~=2.32.5

SQLAlchemy>=2.0
asyncpg>=0.29
alembic>=1.13
psycopg2-binary>=2.9

# Tests (SQLite in-memory via aiosqlite in conftest)
pytest>=8.0
pytest-asyncio>=0.24
httpx>=0.27
aiosqlite>=0.20
